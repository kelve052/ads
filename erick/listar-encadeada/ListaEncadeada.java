public class ListaEncadeada {
     public Elemento  primeiro;
     public Elemento fim;
     public int tamanho;

     public ListaEncadeada(Elemento primeiro, Elemento fim, int tamanho) {
     this.primeiro = primeiro;
     this.fim = fim;
     this.tamanho = tamanho;
     }

     public Integer get(int posicao) {
          if(tamanho == 0){
               return null;
          }
     }
}